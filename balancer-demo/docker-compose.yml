version: '3.8'

services:
  # FastAPI Instance A
  gateway-a:
    build: .
    environment:
      - INSTANCE_NAME=Instancia-A
    expose:
      - "8000"

  # FastAPI Instance B
  gateway-b:
    build: .
    environment:
      - INSTANCE_NAME=Instancia-B
    expose:
      - "8000"

  # Nginx Load Balancer
  balancer:
    image: nginx:alpine
    ports:
      - "8080:8080"
    volumes:
      - ./nginx.docker.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - gateway-a
      - gateway-b
